---

**@file**：YYC³-用户操作手册
**@description**：YYC³餐饮行业智能化平台的用户操作手册
**@author**：YYC³
**@version**：v1.0.0
**@created**：2025-01-30
**@updated**：2025-01-30
**@status**：published
**@tags**：YYC³,文档

---
# 🔖 YYC³ 用户操作手册

> ***YanYuCloudCube***
> **标语**：言启象限 | 语枢未来
> ***Words Initiate Quadrants, Language Serves as Core for the Future***
> **标语**：万象归元于云枢 | 深栈智启新纪元
> ***All things converge in the cloud pivot; Deep stacks ignite a new era of intelligence***

---

## 📋 文档信息

| 属性 | 内容 |
|------|------|
| **文档标题** | YYC³ 用户操作手册 |
| **文档类型** | 用户指南文档 |
| **所属阶段** | 用户使用 |
| **遵循规范** | YYC³ 团队标准化规范 v1.0.0 |
| **版本号** | v1.0.0 |
| **创建日期** | 2025-01-30 |
| **作者** | YYC³ Team |
| **更新日期** | 2025-01-30 |

---

## 📑 目录

1. [系统概述](#1-系统概述)
2. [快速开始](#2-快速开始)
3. [账号管理](#3-账号管理)
4. [商品管理](#4-商品管理)
5. [订单管理](#5-订单管理)
6. [库存管理](#6-库存管理)
7. [客户管理](#7-客户管理)
8. [营销管理](#8-营销管理)
9. [财务管理](#9-财务管理)
10. [数据报表](#10-数据报表)
11. [系统设置](#11-系统设置)
12. [常见问题](#12-常见问题)

---

## 1. 概述

### 1.1 说明

本文档是YYC³餐饮行业智能化平台文档体系的重要组成部分，旨在提供清晰、完整、准确的信息。

通过本文档，读者可以：
- 了解相关概念和背景
- 掌握核心内容和要点
- 获得实用的指导和帮助
- 参考相关的资源和资料

文档遵循YYC³团队标准化规范，确保内容质量和一致性。

### 1.2 目标

本文档的主要目标包括：

- **信息传递**：准确传递相关信息和知识
- **指导实践**：提供实用的指导和参考
- **降低成本**：减少沟通成本和学习成本
- **提高效率**：帮助读者快速理解和应用

通过实现这些目标，文档能够为项目的成功做出重要贡献。

### 1.3 范围

本文档的适用范围：

- **适用对象**：开发人员、测试人员、运维人员、产品经理等
- **适用阶段**：开发、测试、部署、运维等各个阶段
- **适用场景**：日常开发、问题排查、系统维护等

超出本文档范围的内容，请参考其他相关文档。

## 2. 详细内容

### 2.1 核心内容

### 2.2 实现细节

### 2.3 注意事项

## 3. 参考信息

### 3.1 相关文档

### 3.2 参考资料

### 3.3 附录

## 1. 系统概述

### 1.1 系统简介

YYC³ 餐饮管理平台是一款专为餐饮行业打造的全方位管理系统，集成了商品管理、订单管理、库存管理、客户管理、营销管理、财务管理、数据报表等核心功能，帮助餐饮企业实现数字化转型，提升运营效率，降低运营成本。

### 1.2 核心功能

- **商品管理**：商品上架、下架、分类、库存管理
- **订单管理**：订单接收、确认、配送、退款
- **库存管理**：库存调整、预警、盘点
- **客户管理**：客户信息、标签、等级管理
- **营销管理**：优惠券、促销活动、会员管理
- **财务管理**：支付、提现、账单管理
- **数据报表**：销售报表、库存报表、客户报表
- **系统设置**：店铺设置、权限管理、通知设置

### 1.3 技术特点

- **响应式设计**：支持PC、平板、手机多端访问
- **实时同步**：数据实时同步，确保信息准确
- **智能推荐**：基于AI的智能推荐系统
- **安全可靠**：采用银行级安全加密技术
- **易于使用**：简洁直观的操作界面

---

## 2. 快速开始

### 2.1 系统登录

#### 2.1.1 访问系统

打开浏览器，访问系统地址：
```
https://catering.yyc3.com
```

#### 2.1.2 输入账号密码

在登录页面输入：
- 账号：邮箱或手机号
- 密码：8-20位字符

#### 2.1.3 点击登录

点击"登录"按钮，进入系统首页。

### 2.2 系统首页

系统首页包含以下模块：

| 模块 | 说明 |
|------|------|
| **待处理订单** | 显示待确认、待配送的订单数量 |
| **今日销售** | 显示今日销售额和订单量 |
| **库存预警** | 显示库存不足的商品数量 |
| **系统公告** | 显示最新的系统公告 |
| **快捷入口** | 提供常用功能的快捷入口 |

### 2.3 导航菜单

系统左侧为导航菜单，包含以下模块：

- **首页**：系统首页
- **商品管理**：商品相关功能
- **订单管理**：订单相关功能
- **库存管理**：库存相关功能
- **客户管理**：客户相关功能
- **营销管理**：营销相关功能
- **财务管理**：财务相关功能
- **数据报表**：报表相关功能
- **系统设置**：系统设置功能

---

## 3. 账号管理

### 3.1 个人信息

#### 3.1.1 查看个人信息

1. 点击页面右上角的用户头像
2. 在下拉菜单中点击"个人中心"
3. 进入"个人信息"页面
4. 显示个人信息

#### 3.1.2 修改个人信息

1. 在"个人信息"页面点击"编辑"按钮
2. 修改需要更新的信息：
   - 昵称
   - 邮箱
   - 手机号
   - 性别
   - 生日
   - 头像
3. 点击"保存"按钮
4. 系统提示保存成功

### 3.2 账号安全

#### 3.2.1 修改密码

1. 进入"个人中心"页面
2. 点击"账号安全"标签页
3. 点击"修改密码"按钮
4. 输入原密码
5. 输入新密码
6. 确认新密码
7. 点击"确定"按钮
8. 系统提示修改成功

**密码要求**：
- 长度8-20位
- 包含字母和数字
- 不能与原密码相同
- 不能包含用户名

#### 3.2.2 绑定手机号

1. 进入"个人中心"页面
2. 点击"账号安全"标签页
3. 点击"绑定手机号"按钮
4. 输入手机号（11位）
5. 点击"获取验证码"按钮
6. 接收短信验证码（6位数字）
7. 输入验证码
8. 点击"确定"按钮
9. 系统提示绑定成功

#### 3.2.3 绑定邮箱

1. 进入"个人中心"页面
2. 点击"账号安全"标签页
3. 点击"绑定邮箱"按钮
4. 输入邮箱地址
5. 点击"发送验证码"按钮
6. 查收邮件，获取验证码
7. 输入验证码
8. 点击"确定"按钮
9. 系统提示绑定成功

### 3.3 消息通知

#### 3.3.1 查看消息

1. 点击页面右上角的"消息"图标
2. 进入"消息中心"页面
3. 显示所有消息列表

#### 3.3.2 消息类型

- **订单消息**：订单相关通知
- **支付消息**：支付相关通知
- **促销消息**：促销活动通知
- **系统消息**：系统相关通知

#### 3.3.3 消息操作

- **标记已读**：点击消息标记为已读
- **删除消息**：点击删除按钮删除消息
- **全部已读**：点击"全部已读"按钮标记所有消息为已读

---

## 4. 商品管理

### 4.1 商品列表

#### 4.1.1 查看商品列表

1. 点击左侧导航菜单的"商品管理"
2. 进入"商品列表"页面
3. 显示所有商品

#### 4.1.2 搜索商品

在商品列表页面，可以通过以下方式搜索商品：
- **商品名称**：输入商品名称搜索
- **商品分类**：选择商品分类筛选
- **商品状态**：选择商品状态筛选（上架/下架）
- **创建时间**：选择时间范围筛选

#### 4.1.3 商品列表字段

| 字段 | 说明 |
|------|------|
| 商品ID | 商品唯一标识 |
| 商品名称 | 商品名称 |
| 商品分类 | 商品所属分类 |
| 商品价格 | 商品售价 |
| 商品库存 | 当前库存数量 |
| 商品状态 | 上架/下架 |
| 创建时间 | 商品创建时间 |
| 操作 | 编辑、删除、上架、下架 |

### 4.2 添加商品

#### 4.2.1 添加商品步骤

1. 在商品列表页面点击"添加商品"按钮
2. 进入"添加商品"页面
3. 填写商品信息：
   - **基本信息**：商品名称、商品分类、商品价格、商品库存
   - **商品描述**：商品详细描述
   - **商品图片**：上传商品图片
   - **商品规格**：设置商品规格（可选）
4. 点击"保存"按钮
5. 系统提示添加成功

#### 4.2.2 商品信息填写规范

**基本信息**：
- 商品名称：2-200个字符，不能包含特殊字符
- 商品分类：必填，从下拉列表中选择
- 商品价格：必填，必须大于0，保留两位小数
- 商品库存：必填，必须大于等于0，整数

**商品描述**：
- 0-5000个字符
- 支持富文本编辑
- 可以插入图片、视频

**商品图片**：
- 支持 JPG、PNG、GIF 格式
- 建议尺寸 800x800 像素
- 单张图片不超过 5MB
- 最多上传 9 张图片

**商品规格**：
- 规格名称：如颜色、尺寸
- 规格值：如红色、蓝色、M、L
- 规格价格：不同规格可以设置不同价格
- 规格库存：不同规格可以设置不同库存

### 4.3 编辑商品

#### 4.3.1 编辑商品步骤

1. 在商品列表中找到需要编辑的商品
2. 点击"编辑"按钮
3. 进入"编辑商品"页面
4. 修改商品信息
5. 点击"保存"按钮
6. 系统提示保存成功

#### 4.3.2 编辑限制

- 商品ID不能修改
- 商品创建时间不能修改
- 商品销量不能修改（系统自动统计）

### 4.4 上架/下架商品

#### 4.4.1 上架商品

1. 在商品列表中找到需要上架的商品
2. 点击"上架"按钮
3. 系统提示确认操作
4. 点击"确定"按钮
5. 系统提示上架成功
6. 商品状态变为"上架"

#### 4.4.2 下架商品

1. 在商品列表中找到需要下架的商品
2. 点击"下架"按钮
3. 系统提示确认操作
4. 点击"确定"按钮
5. 系统提示下架成功
6. 商品状态变为"下架"

#### 4.4.3 批量上架/下架

1. 在商品列表中勾选需要操作的商品
2. 点击"批量操作"按钮
3. 选择"批量上架"或"批量下架"
4. 系统提示确认操作
5. 点击"确定"按钮
6. 系统提示操作成功

### 4.5 删除商品

#### 4.5.1 删除商品步骤

1. 在商品列表中找到需要删除的商品
2. 点击"删除"按钮
3. 系统弹出确认对话框
4. 点击"确定"按钮
5. 系统提示删除成功

#### 4.5.2 删除限制

- 有未完成订单的商品不能删除
- 建议先下架商品，确认无订单后再删除

### 4.6 商品分类

#### 4.6.1 查看商品分类

1. 点击左侧导航菜单的"商品管理"
2. 点击"商品分类"子菜单
3. 进入"商品分类"页面
4. 显示所有商品分类

#### 4.6.2 添加商品分类

1. 在商品分类页面点击"添加分类"按钮
2. 填写分类信息：
   - 分类名称（必填）
   - 父分类（选填）
   - 排序序号（选填）
   - 分类图标（选填）
3. 点击"保存"按钮
4. 系统提示添加成功

#### 4.6.3 编辑商品分类

1. 在商品分类列表中找到需要编辑的分类
2. 点击"编辑"按钮
3. 修改分类信息
4. 点击"保存"按钮
5. 系统提示保存成功

#### 4.6.4 删除商品分类

1. 在商品分类列表中找到需要删除的分类
2. 点击"删除"按钮
3. 系统弹出确认对话框
4. 点击"确定"按钮
5. 系统提示删除成功

**删除限制**：
- 有子分类的分类不能删除
- 有商品的分类不能删除

---

## 5. 订单管理

### 5.1 订单列表

#### 5.1.1 查看订单列表

1. 点击左侧导航菜单的"订单管理"
2. 进入"订单列表"页面
3. 显示所有订单

#### 5.1.2 搜索订单

在订单列表页面，可以通过以下方式搜索订单：
- **订单号**：输入订单号搜索
- **用户昵称**：输入用户昵称搜索
- **用户手机号**：输入用户手机号搜索
- **订单状态**：选择订单状态筛选
- **支付状态**：选择支付状态筛选
- **创建时间**：选择时间范围筛选

#### 5.1.3 订单状态

| 状态 | 说明 |
|------|------|
| 待确认 | 用户已下单，等待商家确认 |
| 待支付 | 订单已确认，等待用户支付 |
| 待配送 | 订单已支付，等待配送 |
| 配送中 | 订单正在配送中 |
| 已完成 | 订单已完成 |
| 已取消 | 订单已取消 |
| 已退款 | 订单已退款 |

#### 5.1.4 订单列表字段

| 字段 | 说明 |
|------|------|
| 订单号 | 订单唯一标识 |
| 用户昵称 | 下单用户昵称 |
| 用户手机号 | 下单用户手机号 |
| 商品信息 | 订单包含的商品信息 |
| 订单金额 | 订单总金额 |
| 订单状态 | 订单当前状态 |
| 支付状态 | 支付状态 |
| 创建时间 | 订单创建时间 |
| 操作 | 查看详情、确认订单、取消订单、申请退款 |

### 5.2 订单详情

#### 5.2.1 查看订单详情

1. 在订单列表中找到需要查看的订单
2. 点击"查看详情"按钮
3. 进入"订单详情"页面
4. 显示订单完整信息

#### 5.2.2 订单详情内容

**订单基本信息**：
- 订单号
- 订单状态
- 支付状态
- 创建时间
- 更新时间

**用户信息**：
- 用户昵称
- 用户手机号
- 用户邮箱
- 用户地址

**商品信息**：
- 商品列表
- 商品数量
- 商品单价
- 商品小计

**金额信息**：
- 商品总额
- 优惠金额
- 运费
- 实付金额

**支付信息**：
- 支付方式
- 支付时间
- 交易号

**配送信息**：
- 配送地址
- 配送时间
- 配送人员
- 配送电话

**备注信息**：
- 订单备注
- 商家备注

### 5.3 确认订单

#### 5.3.1 确认订单步骤

1. 在订单列表中找到待确认的订单
2. 点击"确认订单"按钮
3. 系统弹出确认对话框
4. 填写商家备注（可选）
5. 点击"确定"按钮
6. 系统提示确认成功
7. 订单状态变为"待支付"

#### 5.3.2 确认订单注意事项

- 确认前请仔细核对订单信息
- 确认后订单将进入支付流程
- 如有异常情况，请联系客户
- 建议及时确认订单（建议10分钟内）

### 5.4 取消订单

#### 5.4.1 取消订单步骤

1. 在订单列表中找到需要取消的订单
2. 点击"取消订单"按钮
3. 系统弹出取消对话框
4. 选择取消原因：
   - 用户主动取消
   - 商品缺货
   - 配送范围外
   - 其他原因
5. 填写取消说明（可选）
6. 点击"确定"按钮
7. 系统提示取消成功
8. 订单状态变为"已取消"

#### 5.4.2 取消订单限制

- 已支付的订单需要先退款才能取消
- 配送中的订单不能取消
- 已完成的订单不能取消

### 5.5 申请退款

#### 5.5.1 申请退款步骤

1. 在订单列表中找到需要退款的订单
2. 点击"申请退款"按钮
3. 系统弹出退款对话框
4. 选择退款类型：
   - 全额退款
   - 部分退款
5. 填写退款金额
6. 选择退款原因：
   - 商品问题
   - 配送问题
   - 服务问题
   - 其他原因
7. 填写退款说明（可选）
8. 点击"提交"按钮
9. 系统提示提交成功
10. 等待审核（1-3个工作日）

#### 5.5.2 退款审核流程

1. 用户提交退款申请
2. 商家审核退款申请
3. 审核通过后，系统发起退款
4. 支付平台处理退款
5. 退款到账（1-3个工作日）
6. 系统通知用户退款结果

#### 5.5.3 退款状态

| 状态 | 说明 |
|------|------|
| 待审核 | 退款申请已提交，等待审核 |
| 审核通过 | 退款申请审核通过，等待退款 |
| 退款中 | 退款正在处理中 |
| 退款成功 | 退款已到账 |
| 退款失败 | 退款失败，请联系客服 |
| 审核拒绝 | 退款申请被拒绝 |

### 5.6 安排配送

#### 5.6.1 安排配送步骤

1. 在订单列表中找到待配送的订单
2. 点击"安排配送"按钮
3. 系统弹出配送对话框
4. 选择配送人员
5. 设置预计配送时间
6. 点击"确定"按钮
7. 系统提示安排成功
8. 订单状态变为"配送中"

#### 5.6.2 配送人员选择

- 从配送人员列表中选择
- 可以查看配送人员的工作状态
- 可以查看配送人员的配送记录

#### 5.6.3 预计配送时间

- 根据配送距离和路况估算
- 建议预留充足时间
- 可以根据实际情况调整

### 5.7 导出订单

#### 5.7.1 导出订单步骤

1. 在订单列表页面
2. 设置筛选条件（可选）
3. 点击"导出"按钮
4. 选择导出格式：
   - Excel
   - CSV
5. 点击"确定"按钮
6. 系统生成文件
7. 自动下载到本地

#### 5.7.2 导出内容

- 订单基本信息
- 用户信息
- 商品信息
- 金额信息
- 支付信息
- 配送信息

#### 5.7.3 导出注意事项

- 一次最多导出10000条订单
- 导出时间取决于数据量
- 建议在非高峰时段导出

---

## 6. 库存管理

### 6.1 库存列表

#### 6.1.1 查看库存列表

1. 点击左侧导航菜单的"库存管理"
2. 进入"库存列表"页面
3. 显示所有商品的库存信息

#### 6.1.2 库存列表字段

| 字段 | 说明 |
|------|------|
| 商品ID | 商品唯一标识 |
| 商品名称 | 商品名称 |
| 商品分类 | 商品所属分类 |
| 当前库存 | 当前库存数量 |
| 预警库存 | 预警库存值 |
| 库存状态 | 正常/预警 |
| 最后更新 | 最后更新时间 |

#### 6.1.3 库存状态

- **正常**：库存充足
- **预警**：库存低于预警值

### 6.2 调整库存

#### 6.2.1 调整库存步骤

1. 点击左侧导航菜单的"库存管理"
2. 进入"库存列表"页面
3. 找到需要调整的商品
4. 点击"调整库存"按钮
5. 系统弹出调整对话框
6. 选择调整类型：
   - 入库（增加库存）
   - 出库（减少库存）
   - 盘点（设置库存）
7. 输入调整数量
8. 填写调整原因（必填）
9. 点击"确定"按钮
10. 系统提示调整成功

#### 6.2.2 调整类型说明

- **入库**：采购入库、退货入库
- **出库**：销售出库、损耗出库
- **盘点**：实际盘点、系统修正

### 6.3 设置库存预警

#### 6.3.1 设置库存预警步骤

1. 在库存列表中找到需要设置预警的商品
2. 点击"设置预警"按钮
3. 系统弹出预警对话框
4. 输入预警库存值
5. 点击"确定"按钮
6. 系统提示设置成功

#### 6.3.2 预警通知

当库存低于预警值时，系统会：
- 在库存列表中显示预警状态（黄色）
- 发送站内消息通知
- 发送邮件通知（如已配置）
- 发送短信通知（如已配置）

#### 6.3.3 预警值设置建议

- 根据日均销量设置
- 建议设置为3-7天的销量
- 可以根据实际情况调整

### 6.4 库存记录

#### 6.4.1 查看库存记录

1. 点击左侧导航菜单的"库存管理"
2. 点击"库存记录"子菜单
3. 进入"库存记录"页面
4. 显示所有库存调整记录

#### 6.4.2 库存记录字段

| 字段 | 说明 |
|------|------|
| 记录ID | 记录唯一标识 |
| 商品ID | 商品唯一标识 |
| 商品名称 | 商品名称 |
| 调整类型 | 入库/出库/盘点 |
| 调整数量 | 调整数量 |
| 调整前库存 | 调整前库存数量 |
| 调整后库存 | 调整后库存数量 |
| 调整原因 | 调整原因 |
| 操作人 | 操作人 |
| 操作时间 | 操作时间 |

#### 6.4.3 搜索和筛选

- 支持按商品名称、调整原因搜索
- 支持按调整类型、时间范围筛选

---

## 7. 客户管理

### 7.1 客户列表

#### 7.1.1 查看客户列表

1. 点击左侧导航菜单的"客户管理"
2. 进入"客户列表"页面
3. 显示所有客户

#### 7.1.2 搜索客户

在客户列表页面，可以通过以下方式搜索客户：
- **客户昵称**：输入客户昵称搜索
- **客户手机号**：输入客户手机号搜索
- **客户等级**：选择客户等级筛选
- **注册时间**：选择时间范围筛选

#### 7.1.3 客户列表字段

| 字段 | 说明 |
|------|------|
| 客户ID | 客户唯一标识 |
| 客户昵称 | 客户昵称 |
| 客户手机号 | 客户手机号 |
| 客户等级 | 客户等级 |
| 订单数 | 累计订单数 |
| 消费金额 | 累计消费金额 |
| 注册时间 | 注册时间 |
| 操作 | 查看详情、添加标签 |

### 7.2 客户详情

#### 7.2.1 查看客户详情

1. 在客户列表中找到需要查看的客户
2. 点击"查看详情"按钮
3. 进入"客户详情"页面
4. 显示客户完整信息

#### 7.2.2 客户详情内容

**基本信息**：
- 客户昵称
- 客户手机号
- 客户邮箱
- 客户性别
- 客户生日

**账户信息**：
- 注册时间
- 最后登录
- 客户等级

**订单信息**：
- 订单数
- 消费金额
- 平均客单价

**地址信息**：
- 收货地址列表

**标签信息**：
- 客户标签

### 7.3 客户标签

#### 7.3.1 添加客户标签

1. 进入客户详情页面
2. 点击"添加标签"按钮
3. 输入标签名称
4. 点击"确定"按钮
5. 系统提示添加成功

#### 7.3.2 删除客户标签

1. 在客户详情页面找到需要删除的标签
2. 点击标签右侧的"删除"按钮
3. 系统弹出确认对话框
4. 点击"确定"按钮
5. 系统提示删除成功

#### 7.3.3 常用标签

- 高价值客户
- 活跃客户
- 潜力客户
- 新客户
- 老客户
- VIP客户

### 7.4 客户等级

#### 7.4.1 等级规则

| 等级 | 消费金额 | 优惠权益 |
|------|---------|---------|
| 青铜会员 | 0-999元 | 无 |
| 白银会员 | 1000-4999元 | 9.8折 |
| 黄金会员 | 5000-19999元 | 9.5折 |
| 铂金会员 | 20000元以上 | 9折 |

#### 7.4.2 升级规则

- 每天凌晨自动计算客户等级
- 达到升级条件后自动升级
- 升级后发送通知消息
- 等级只能升级不能降级

---

## 8. 营销管理

### 8.1 优惠券

#### 8.1.1 优惠券列表

1. 点击左侧导航菜单的"营销管理"
2. 点击"优惠券"子菜单
3. 进入"优惠券列表"页面
4. 显示所有优惠券

#### 8.1.2 创建优惠券

1. 在优惠券列表页面点击"创建优惠券"按钮
2. 填写优惠券信息：
   - 优惠券名称（必填）
   - 优惠券类型（必填）
   - 面值（必填）
   - 使用门槛（选填）
   - 总数量（必填）
   - 有效期（必填）
3. 点击"保存"按钮
4. 系统提示创建成功

#### 8.1.3 优惠券类型

- **满减券**：满X元减Y元
- **折扣券**：享受X折优惠
- **立减券**：直接减Y元

#### 8.1.4 发放优惠券

1. 在优惠券列表中找到需要发放的优惠券
2. 点击"发放"按钮
3. 系统弹出发放对话框
4. 选择发放对象：
   - 全部用户
   - 指定用户
   - 指定等级用户
5. 点击"确定"按钮
6. 系统提示发放成功

### 8.2 促销活动

#### 8.2.1 促销活动列表

1. 点击左侧导航菜单的"营销管理"
2. 点击"促销活动"子菜单
3. 进入"促销活动列表"页面
4. 显示所有促销活动

#### 8.2.2 创建促销活动

1. 在促销活动列表页面点击"创建活动"按钮
2. 填写活动信息：
   - 活动名称（必填）
   - 活动类型（必填）
   - 活动时间（必填）
   - 活动规则（必填）
3. 选择参与商品
4. 点击"保存"按钮
5. 系统提示创建成功

#### 8.2.3 活动类型

- **满减活动**：满X元减Y元
- **折扣活动**：享受X折优惠
- **秒杀活动**：限时特价

---

## 9. 财务管理

### 9.1 支付记录

#### 9.1.1 查看支付记录

1. 点击左侧导航菜单的"财务管理"
2. 点击"支付记录"子菜单
3. 进入"支付记录"页面
4. 显示所有支付记录

#### 9.1.2 支付记录字段

| 字段 | 说明 |
|------|------|
| 交易号 | 交易唯一标识 |
| 订单号 | 订单号 |
| 支付方式 | 支付方式 |
| 支付金额 | 支付金额 |
| 支付状态 | 支付状态 |
| 支付时间 | 支付时间 |

### 9.2 提现

#### 9.2.1 申请提现

1. 点击左侧导航菜单的"财务管理"
2. 点击"提现"子菜单
3. 进入"提现"页面
4. 选择提现银行卡
5. 输入提现金额
6. 确认提现信息
7. 提交提现申请
8. 等待到账（1-3个工作日）

#### 9.2.2 提现规则

- 最低提现金额：100元
- 每日提现次数：最多3次
- 提现手续费：0元（平台承担）
- 到账时间：1-3个工作日

---

## 10. 数据报表

### 10.1 销售报表

#### 10.1.1 查看销售报表

1. 点击左侧导航菜单的"数据报表"
2. 点击"销售报表"子菜单
3. 进入"销售报表"页面
4. 显示销售数据统计

#### 10.1.2 销售报表指标

| 指标 | 说明 |
|------|------|
| 销售额 | 累计销售额 |
| 订单量 | 累计订单量 |
| 客单价 | 平均客单价 |
| 复购率 | 客户复购率 |
| 退款率 | 订单退款率 |

#### 10.1.3 时间范围选择

- 今日
- 昨日
- 近7天
- 近30天
- 自定义

### 10.2 导出报表

#### 10.2.1 导出报表步骤

1. 在报表页面
2. 选择时间范围
3. 点击"导出"按钮
4. 选择导出格式：
   - Excel
   - PDF
5. 点击"确定"按钮
6. 系统生成文件
7. 自动下载到本地

#### 10.2.2 导出注意事项

- 一次最多导出10000条数据
- 导出时间取决于数据量
- 建议在非高峰时段导出

---

## 11. 系统设置

### 11.1 店铺设置

#### 11.1.1 修改店铺信息

1. 点击左侧导航菜单的"系统设置"
2. 点击"店铺设置"子菜单
3. 进入"店铺信息"页面
4. 点击"编辑"按钮
5. 修改需要更新的信息：
   - 店铺名称
   - 店铺类型
   - 联系电话
   - 店铺地址
   - 营业时间
   - 店铺简介
6. 点击"保存"按钮
7. 系统提示保存成功

### 11.2 通知设置

#### 11.2.1 配置通知方式

1. 点击左侧导航菜单的"系统设置"
2. 点击"通知设置"子菜单
3. 进入"通知设置"页面
4. 配置通知方式：
   - 短信通知
   - 邮件通知
   - 微信通知
5. 点击"保存"按钮

#### 11.2.2 通知类型

- 订单通知
- 支付通知
- 促销通知
- 系统通知

### 11.3 权限设置

#### 11.3.1 创建角色

1. 点击左侧导航菜单的"系统设置"
2. 点击"权限设置"子菜单
3. 进入"角色管理"页面
4. 点击"创建角色"按钮
5. 填写角色信息：
   - 角色名称（必填）
   - 角色编码（必填）
   - 角色描述（选填）
6. 分配权限
7. 点击"保存"按钮
8. 系统提示创建成功

---

## 12. 常见问题

### 12.1 账号问题

**Q: 忘记密码怎么办？**

A: 在登录页面点击"忘记密码"，选择邮箱重置或手机重置，按照提示操作即可。

**Q: 如何修改密码？**

A: 进入"个人中心"页面，点击"账号安全"标签页，点击"修改密码"按钮，按照提示操作即可。

### 12.2 商品问题

**Q: 商品图片上传失败怎么办？**

A: 请检查图片格式（支持JPG、PNG、GIF）、图片大小（不超过5MB）、图片尺寸（建议800x800像素），如果以上都符合，请联系客服。

**Q: 如何批量上架商品？**

A: 在商品列表中勾选需要上架的商品，点击"批量操作"按钮，选择"批量上架"即可。

### 12.3 订单问题

**Q: 如何取消订单？**

A: 在订单列表中找到需要取消的订单，点击"取消订单"按钮，选择取消原因，点击"确定"即可。

**Q: 如何申请退款？**

A: 在订单列表中找到需要退款的订单，点击"申请退款"按钮，选择退款类型和原因，提交申请即可。

### 12.4 技术支持

**Q: 如何联系客服？**

A: 您可以通过以下方式联系客服：
- 在线客服：点击页面右下角的"在线客服"图标
- 电话客服：400-123-4567
- 邮件客服：support@yyc3.com
- 工单系统：进入"帮助中心"页面，点击"提交工单"

---

## 📄 文档标尾 (Footer)

> 「***YanYuCloudCube***」
> 「***<admin@0379.email>***」
> 「***Words Initiate Quadrants, Language Serves as Core for the Future***」
> 「***All things converge in the cloud pivot; Deep stacks ignite a new era of intelligence***」




## 概述

### 架构概述

本架构文档详细描述了系统的整体架构设计，包括架构目标、设计原则、技术选型等关键信息。

#### 架构目标

- **高可用性**：确保系统稳定运行，故障自动恢复
- **高性能**：响应迅速，资源利用高效
- **高安全性**：数据加密，权限严格控制
- **高扩展性**：模块化设计，易于功能扩展
- **高可维护性**：代码清晰，文档完善

#### 设计原则

- **单一职责**：每个组件只负责一个功能
- **开闭原则**：对扩展开放，对修改关闭
- **依赖倒置**：依赖抽象而非具体实现
- **接口隔离**：使用细粒度的接口
- **迪米特法则**：最少知识原则



## 架构设计

### 架构设计

#### 整体架构

系统采用分层架构设计，包括：

- **表现层**：负责用户界面和交互
- **应用层**：处理业务逻辑
- **业务层**：实现核心业务功能
- **数据层**：管理数据存储和访问
- **基础设施层**：提供基础服务支持

#### 模块划分

系统划分为多个独立模块，每个模块负责特定功能：

- **用户模块**：用户管理和认证
- **订单模块**：订单处理和管理
- **支付模块**：支付集成和处理
- **通知模块**：消息通知和推送
- **报表模块**：数据统计和分析

#### 技术选型

- **前端框架**：React / Vue
- **后端框架**：Node.js / Express / Fastify
- **数据库**：PostgreSQL / MongoDB
- **缓存**：Redis
- **消息队列**：RabbitMQ / Kafka



## 技术实现

### 技术实现

#### 核心技术栈

```typescript
// 核心依赖
{
  "dependencies": {
    "react": "^18.0.0",
    "typescript": "^5.0.0",
    "express": "^4.18.0",
    "prisma": "^5.0.0",
    "redis": "^4.6.0"
  }
}
```

#### 关键实现

1. **服务层实现**
```typescript
class UserService {
  async createUser(data: CreateUserDto): Promise<User> {
    // 验证输入
    this.validateUserData(data);
    
    // 加密密码
    const hashedPassword = await this.hashPassword(data.password);
    
    // 创建用户
    const user = await this.userRepository.create({
      ...data,
      password: hashedPassword
    });
    
    return user;
  }
}
```

2. **中间件实现**
```typescript
const authMiddleware = async (req: Request, res: Response, next: NextFunction) => {
  const token = req.headers.authorization?.split(' ')[1];
  
  if (!token) {
    return res.status(401).json({ error: '未授权访问' });
  }
  
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch (error) {
    return res.status(401).json({ error: '令牌无效' });
  }
};
```



## 部署方案

### 部署方案

#### 部署架构

采用容器化部署方案，使用Docker和Kubernetes进行编排。

#### 部署步骤

1. **环境准备**
```bash
# 安装Docker
curl -fsSL https://get.docker.com | sh

# 安装Kubernetes
# 根据操作系统选择相应的安装方式
```

2. **构建镜像**
```bash
# 构建应用镜像
docker build -t yyc3-app:latest .

# 推送到镜像仓库
docker push registry.example.com/yyc3-app:latest
```

3. **部署到Kubernetes**
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: yyc3-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: yyc3-app
  template:
    metadata:
      labels:
        app: yyc3-app
    spec:
      containers:
      - name: app
        image: registry.example.com/yyc3-app:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
```

4. **配置服务**
```yaml
apiVersion: v1
kind: Service
metadata:
  name: yyc3-app-service
spec:
  selector:
    app: yyc3-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer
```



## 性能优化

### 性能优化

#### 前端优化

1. **代码分割**
```typescript
// 路由级别代码分割
const Home = lazy(() => import('./pages/Home'));
const About = lazy(() => import('./pages/About'));

function App() {
  return (
    <Suspense fallback={<Loading />}>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
      </Routes>
    </Suspense>
  );
}
```

2. **缓存策略**
```typescript
// React.memo 避免不必要的重渲染
const MemoizedComponent = React.memo(({ data }) => {
  return <div>{data.value}</div>;
});

// useMemo 缓存计算结果
const expensiveValue = useMemo(() => {
  return computeExpensiveValue(data);
}, [data]);
```

#### 后端优化

1. **数据库优化**
```typescript
// 使用索引
CREATE INDEX idx_user_email ON users(email);

// 查询优化
const users = await prisma.user.findMany({
  select: {
    id: true,
    name: true,
    email: true
  },
  where: {
    active: true
  },
  take: 100
});
```

2. **缓存策略**
```typescript
// Redis缓存
async function getUser(id: string): Promise<User> {
  const cacheKey = `user:${id}`;
  
  // 尝试从缓存获取
  const cached = await redis.get(cacheKey);
  if (cached) {
    return JSON.parse(cached);
  }
  
  // 从数据库获取
  const user = await prisma.user.findUnique({ where: { id } });
  
  // 写入缓存
  await redis.setex(cacheKey, 3600, JSON.stringify(user));
  
  return user;
}
```



## 安全考虑

### 安全考虑

#### 认证与授权

1. **JWT认证**
```typescript
// 生成JWT令牌
const token = jwt.sign(
  { userId: user.id, role: user.role },
  process.env.JWT_SECRET,
  { expiresIn: '24h' }
);

// 验证JWT令牌
const decoded = jwt.verify(token, process.env.JWT_SECRET);
```

2. **RBAC授权**
```typescript
// 角色权限检查
function checkPermission(user: User, resource: string, action: string): boolean {
  const permissions = rolePermissions[user.role];
  return permissions.some(p => 
    p.resource === resource && p.actions.includes(action)
  );
}
```

#### 数据保护

1. **输入验证**
```typescript
// 使用Zod进行输入验证
const createUserSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8).regex(/[A-Z]/),
  name: z.string().min(2)
});

const validated = createUserSchema.parse(input);
```

2. **数据加密**
```typescript
// 使用bcrypt加密密码
const hashedPassword = await bcrypt.hash(password, 10);

// 验证密码
const isValid = await bcrypt.compare(password, hashedPassword);
```

#### 安全头配置

```typescript
// Express安全头配置
app.use(helmet());
app.use(cors({
  origin: process.env.ALLOWED_ORIGINS?.split(','),
  credentials: true
}));
```



## 监控告警

### 监控告警

#### 监控指标

1. **系统指标**
- CPU使用率
- 内存使用率
- 磁盘使用率
- 网络I/O

2. **应用指标**
- 请求量(RPS)
- 响应时间
- 错误率
- 并发用户数

3. **业务指标**
- 用户注册数
- 订单创建数
- 支付成功率
- 用户活跃度

#### 监控工具

```typescript
// Prometheus指标收集
import { Counter, Histogram, Gauge } from 'prom-client';

const requestCounter = new Counter({
  name: 'http_requests_total',
  help: 'Total number of HTTP requests',
  labelNames: ['method', 'route', 'status']
});

const responseTime = new Histogram({
  name: 'http_request_duration_seconds',
  help: 'HTTP request duration in seconds',
  labelNames: ['method', 'route']
});

// 使用中间件记录指标
app.use((req, res, next) => {
  const start = Date.now();
  
  res.on('finish', () => {
    const duration = (Date.now() - start) / 1000;
    requestCounter.inc({
      method: req.method,
      route: req.route?.path || req.path,
      status: res.statusCode
    });
    responseTime.observe({
      method: req.method,
      route: req.route?.path || req.path
    }, duration);
  });
  
  next();
});
```

#### 告警规则

```yaml
groups:
- name: api_alerts
  rules:
  - alert: HighErrorRate
    expr: rate(http_requests_total{status=~"5.."}[5m]) > 0.05
    for: 5m
    labels:
      severity: critical
    annotations:
      summary: "API错误率过高"
      description: "5分钟内错误率超过5%"
  
  - alert: HighResponseTime
    expr: histogram_quantile(0.95, http_request_duration_seconds) > 1
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: "API响应时间过长"
      description: "95%分位响应时间超过1秒"
```



## 最佳实践

### 最佳实践

#### 代码规范

1. **命名规范**
```typescript
// 变量：camelCase
const userName = 'John';

// 常量：UPPER_SNAKE_CASE
const MAX_RETRY_COUNT = 3;

// 类：PascalCase
class UserService { }

// 接口：PascalCase，前缀I（可选）
interface IUserService { }
```

2. **注释规范**
```typescript
/**
 * 创建用户
 * @param email - 用户邮箱
 * @param password - 用户密码
 * @returns 创建的用户对象
 * @throws {Error} 当邮箱已存在时抛出错误
 */
async function createUser(
  email: string, 
  password: string
): Promise<User> {
  // 实现
}
```

#### 错误处理

```typescript
// 统一错误处理
class AppError extends Error {
  constructor(
    public statusCode: number,
    public message: string,
    public isOperational = true
  ) {
    super(message);
    this.name = this.constructor.name;
    Error.captureStackTrace(this, this.constructor);
  }
}

// 使用错误处理中间件
app.use((err: Error, req: Request, res: Response, next: NextFunction) => {
  if (err instanceof AppError) {
    return res.status(err.statusCode).json({
      success: false,
      error: err.message
    });
  }
  
  // 记录未预期的错误
  logger.error('Unexpected error:', err);
  
  return res.status(500).json({
    success: false,
    error: '服务器内部错误'
  });
});
```

#### 日志记录

```typescript
// 结构化日志
import winston from 'winston';

const logger = winston.createLogger({
  level: 'info',
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.json()
  ),
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' })
  ]
});

// 使用日志
logger.info('User created', { userId: user.id, email: user.email });
logger.error('Database connection failed', { error: error.message });
```



## 最佳实践

### 最佳实践

#### 代码规范

1. **命名规范**
```typescript
// 变量：camelCase
const userName = 'John';

// 常量：UPPER_SNAKE_CASE
const MAX_RETRY_COUNT = 3;

// 类：PascalCase
class UserService { }

// 接口：PascalCase，前缀I（可选）
interface IUserService { }
```

2. **注释规范**
```typescript
/**
 * 创建用户
 * @param email - 用户邮箱
 * @param password - 用户密码
 * @returns 创建的用户对象
 * @throws {Error} 当邮箱已存在时抛出错误
 */
async function createUser(
  email: string, 
  password: string
): Promise<User> {
  // 实现
}
```

#### 错误处理

```typescript
// 统一错误处理
class AppError extends Error {
  constructor(
    public statusCode: number,
    public message: string,
    public isOperational = true
  ) {
    super(message);
    this.name = this.constructor.name;
    Error.captureStackTrace(this, this.constructor);
  }
}

// 使用错误处理中间件
app.use((err: Error, req: Request, res: Response, next: NextFunction) => {
  if (err instanceof AppError) {
    return res.status(err.statusCode).json({
      success: false,
      error: err.message
    });
  }
  
  // 记录未预期的错误
  logger.error('Unexpected error:', err);
  
  return res.status(500).json({
    success: false,
    error: '服务器内部错误'
  });
});
```

#### 日志记录

```typescript
// 结构化日志
import winston from 'winston';

const logger = winston.createLogger({
  level: 'info',
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.json()
  ),
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' })
  ]
});

// 使用日志
logger.info('User created', { userId: user.id, email: user.email });
logger.error('Database connection failed', { error: error.message });
```


## 相关文档

- [🔖 YYC³ 用户快速入门指南](YYC3-Cater-用户指南/架构类/01-YYC3-Cater--架构类-用户快速入门指南.md) - YYC3-Cater-用户指南/架构类
- [🔖 YYC³ 用户常见问题解答](YYC3-Cater-用户指南/技巧类/01-YYC3-Cater--技巧类-用户常见问题解答.md) - YYC3-Cater-用户指南/技巧类
- [🔖 YYC³ 智能化应用业务架构说明书](YYC3-Cater-需求规划/架构类/01-YYC3-Cater--架构类-智能化应用业务架构说明书.md) - YYC3-Cater-需求规划/架构类
- [🔖 YYC³ 多环境部署架构差异文档](YYC3-Cater-部署发布/架构类/03-YYC3-Cater--架构类-多环境部署架构差异文档.md) - YYC3-Cater-部署发布/架构类
- [🔖 YYC³ 智能化能力需求规格说明书](YYC3-Cater-需求规划/架构类/04-YYC3-Cater--架构类-智能化能力需求规格说明书.md) - YYC3-Cater-需求规划/架构类
